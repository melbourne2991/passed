'use strict';


// Declare app level module which depends on filters, and services
angular.module('TestListingsApp', [
  'ui.router',
  'TestListingsApp.filters',
  'TestListingsApp.services',
  'TestListingsApp.directives',
  'TestListingsApp.controllers'
]).
config(['$stateProvider', function($stateProvider) {

}]);
